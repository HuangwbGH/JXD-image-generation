# 过程 Todo

## 目录

- [任务目标](#任务目标)
- [已完成](#已完成)
- [待完成](#待完成)
- [阻塞项](#阻塞项)
- [需要用户配置](#需要用户配置)
- [需要用户提供](#需要用户提供)
- [维护规则](#维护规则)

## 任务目标

阅读并分析项目，按照项目 AI 编程规范整理完善文档，使文档能覆盖当前实现、配置、部署、数据库、测试和后续待办。

## 已完成

- 已识别项目技术栈：Next.js App Router、React、TypeScript、SQLite、Gemini REST、Docker。
- 已梳理核心功能：登录注册、用户审核、部门 API Key、文生图、图生图、生成记录、系统日志、设置页。
- 已新增根目录 `AGENTS.md` 和 `PRD.md`。
- 已新增 `docs/Index.md`、`docs/Config.md`、`docs/Deployment.md`、`docs/DatabaseSchema.md`、`docs/TestReport.md`。
- 已将 README 和 Linux 部署说明改为当前实现版。

## 待完成

- 将 `docker-compose.yml` 的端口映射从固定 `32179:32179` 改为可配置形式，例如 `${PORT:-32179}:32179`。
- 将 `GENERATION_TIMEOUT_SECONDS` 接入 `lib/gemini/generate.ts` 当前固定 120 秒的超时逻辑。
- 补充自动化测试，覆盖登录、权限、设置保存、生成参数校验和记录删除。
- 为设置页的 Gemini 连通性测试补充可见入口或在文档中标注仅 API 可用。
- 评估是否需要为管理员创建用户、审核用户、启停用户增加更明确的操作确认。

## 阻塞项

- 无阻塞项。

## 需要用户配置

- `.env` 中的 `AUTH_SECRET`。
- 服务器访问地址 `APP_BASE_URL`。
- 程序级代理地址。
- 研发一部、研发二部的 Gemini API Key。
- Linux 防火墙或内网访问规则。

## 需要用户提供

- 生产服务器局域网 IP 或内网域名。
- 最终可用的公司代理地址。
- 是否需要保留初始普通用户及其初始密码策略。
- 是否需要对生成记录设置保留周期。

## 维护规则

每次任务完成后更新本文件，至少记录已完成项、遗留风险、阻塞项和需要用户处理的内容。
